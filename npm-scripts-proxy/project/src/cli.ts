#!/usr/bin/env node

import { TDefineOpts } from './typing'

/**
 * 通过提供的方法配置，能带来更好的配置体验
 * @param  {TDefineOpts} config
 * @returns TDefineOpts
 */
export const defineNSPConfig = (config: TDefineOpts) => config
